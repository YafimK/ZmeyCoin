package Blockchain

import (
	"testing"
	)

func TestBlockchain_AddBlock(test *testing.T) {
	//blockchain := NewBlockChain()
	//blockchain.AddBlock("first ZmeyCoinBlock")
	//blockchain.AddBlock("second ZmeyCoinBlock")
	//blockchain.AddBlock("third ZmeyCoinBlock")
	//
	//if len(blockchain.blocks) != blockchain.blocksCount {
	//	test.Errorf("the ZmeyCoinBlock counter isn'test working right got %v but exepected %v",blockchain.blocksCount, len(blockchain.blocks))
	//}
	//
	//if !bytes.Equal(blockchain.blocks[2].Data, []byte("second ZmeyCoinBlock")) {
	//	test.Errorf("The data in the second ZmeyCoinBlock doesn't match the expected data that should be in it")
	//	blockchain.PrintBlockChain()
	//}
}